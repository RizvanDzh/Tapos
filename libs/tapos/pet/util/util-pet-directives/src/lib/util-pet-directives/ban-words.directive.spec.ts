import { BanWordsDirective } from './ban-words.directive';

describe('BanWordsDirective', () => {
    it('should create an instance', () => {
        const directive: BanWordsDirective = new BanWordsDirective();
        expect(directive).toBeTruthy();
    });
});
